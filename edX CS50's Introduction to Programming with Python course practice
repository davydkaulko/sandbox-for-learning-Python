/@Problem Set 1
/Deep Thought 1
/Start
text = input("What is the Answer to the Great Question of Life, the Universe, and Everything? ")
def clean_text(text):
    cleaned = text.strip()
    cleaned = cleaned.lower()
    return cleaned
result_text = clean_text(text)
answers = ["42", "forty two", "forty-two"]
if result_text in answers:
    print("true")
else:
    print("false")
/END
/Home Federal Savings Bank 2
/Start
text = input("Greeting:")
def clean_text(text):
    cleaned = text.strip()
    cleaned = cleaned.lower()
    return cleaned
cleartext = clean_text(text)
if cleartext.startswith("hello"):
      print("0$") 
elif cleartext.startswith("h"):
    print("20$")
else:
  print("100$")
/END
/File Extensions 3 
#get info
text = input("yout file:")
#clean
def clean_text(text):
    cleaned = text.strip().lower()
    return cleaned
cleartext = clean_text(text)
#check for .
if '.' in cleartext:
    extension = cleartext.rsplit('.', 1)[-1]
#extension 
    if extension in ["gif", "jpeg", "png"]:
        print(f"image/{extension}")
    elif extension in ["zip", "pdf"]:
        print(f"application/{extension}")
    elif extension in ["txt"]:
        print("text/plain")
    elif extension in ["jpg"]:
        print("image/jpeg")    
    else:
        print("application/octet-stream")
else:
    print("application/octet-stream")
/END
/Math Interpreter 4 (with some changes frome me)
text = input("")
x, y, z = text.split()
x = int(x)
z = int(z)
if y == "/" :
   if z == 0 :
     print ("can't divide by zero")
   else : 
     result = (x / z)
     print (float(result))
elif y == "*" :
     result = (x * z)
     print (float(result))
elif y == "+" :
    result = (x + z)
    print (float(result))
elif y == "-" :
   result = (x - z)
   print (float(result))
elif y == "**" :
   result = (x ** z)
   print (float(result))
else :
  print  ("unknown operator or invalid input")
/END
/Meal Time 5
# get user input, expected format: "7:30"
text = input("What time is it? ")

def main():
    # convert string time → float hours (e.g. "7:30" → 7.5)
    convertedtext = convert(text) 

    # check meal time ranges (inclusive)
    if 7 <= convertedtext <= 8: 
        print("breakfast time")
    elif 12 <= convertedtext <= 13:
        print("lunch time")
    elif 18 <= convertedtext <= 19:
        print("dinner time")
    else:
        # not required by task, but useful for debugging
        print("It’s not time for a meal")

    return


def convert(text):
    # remove extra spaces
    clear = text.strip()

    # split time into hours and minutes → ["7", "30"]
    dividedtext = clear.split(":")

    # convert both parts to integers
    a = int(dividedtext[0])  # hours
    b = int(dividedtext[1])  # minutes

    # convert to float format
    hours = float(a)
    minutes = float(b / 60)

    # combine into one float value (e.g. 7.5)
    sumhoursminutes = hours + minutes

    return sumhoursminutes


# entry point: run main() only if file is executed directly
if __name__ == "__main__":
    main()

/Meal Time 5* (*=adwanced)

# get user input, expected format: "7:30"
text = input("What time is it? ")
def main():
    # convert string time → float hours (e.g. "7:30" → 7.5)
    convertedtext = convert(text) 
    # check meal time ranges (inclusive)
    if 7 <= convertedtext <= 8: 
        print("breakfast time")
    elif 12 <= convertedtext <= 13:
        print("lunch time")
    elif 18 <= convertedtext <= 19:
        print("dinner time")
    else:
        # not required by task, but useful for debugging
        print("It’s not time for a meal")
    return
def convert(text):
    # remove extra spaces
    clear = text.strip().lower()

    # remove dots to unify formats: p.m. → pm
    clear = clear.replace(".", "")

    # split time into hours and minutes → ["7", "30"]
    if "am" in clear:
        clear = clear.replace("am", "")
        dividedtext = clear.split(":")
        a = int(dividedtext[0])  # hours
        b = int(dividedtext[1])  # minutes

        # special case: 12am → 0
        if a == 12:
            a = 0

    elif "pm" in clear:
        clear = clear.replace("pm", "")
        dividedtext = clear.split(":")
        a = int(dividedtext[0])  # hours
        b = int(dividedtext[1])  # minutes

        # add 12 hours except 12pm
        if a != 12:
            a = a + 12

    else:
        dividedtext = clear.split(":")
        a = int(dividedtext[0])  # hours
        b = int(dividedtext[1])  # minutes

    # convert to float format
    hours = float(a)
    minutes = b / 60

    # combine into one float value (e.g. 7.5)
    sumhoursminutes = hours + minutes

    return sumhoursminutes
# entry point: run main() only if file is executed directly
if __name__ == "__main__":
    main()
/End

/@Problem Set 2
/camelCase 1
/Start
dirtytext = input("")
withoutspace = dirtytext.strip()
result = ""

for char in withoutspace:
    if char.isupper():
        result += "_" + char.lower()
    else:
        result += char.lower()
/End


print(result)
